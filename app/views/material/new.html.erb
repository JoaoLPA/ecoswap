<%= simple_form_for @material, new_users_material_path do |f| %>
  <%= f.association :description, as: :select %>
  <%= f.button :submit, class: "btn btn-primary btn-block" %>
<% end %>
<%= link_to "<", root_path %>
